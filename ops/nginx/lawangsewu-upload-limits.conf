# Include this inside the main server block for the Laravel app.
# It keeps WA Caraka media uploads from being rejected by nginx
# before PHP/Laravel can validate the payload cleanly.

client_max_body_size 25m;

